export enum CursorType {
    Idle = "idle",
    
    Select = "select",
    Info = "info",
    Attack = "attack",
    RangedAttack = "rangedattack",
    Cast = "cast",
    Mount = "mount",
    Dismount = "dismount",
    Fly = "fly",
    Warning = "warning",
    Invalid = "invalid",
}