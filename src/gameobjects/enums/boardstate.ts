export enum BoardState {
    Busy = "busy",
    Idle = "idle",
    View = "view",
    SelectSpell = "select-spell",
    CastSpell = "cast-spell",
    Move = "move",
    Attack = "attack",
    RangedAttack = "ranged-attack",
    Dismount = "dismount",
    GameOver = "game-over",
}
