export enum ActionType {
    None = "none",
    Idle = "idle",
    Invalid = "invalid",
    Select = "select",
    Info = "info",
    Move = "move",
    Attack = "attack",
    RangedAttack = "rangedAttack",
}