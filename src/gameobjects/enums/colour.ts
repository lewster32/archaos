/**
 * Pretty much a replication of the ZX Spectrum colour palette. With an extra
 * colour mostly used to distinguish the raise dead spell notification because
 * it's spooky damnit.
 */
export enum Colour {
    Black,
    Blue,
    Red,
    Magenta,
    Green,
    Cyan,
    Yellow,
    White,
    LightBlue
}